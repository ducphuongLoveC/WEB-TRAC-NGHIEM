// Import statements go first
// Example: import React if you're using React components
import React from 'react';

// Exported constants
export const routesClient = {
    routeConfig: 'clientRoute'
};

export const routesAdmin = {
    tests: 'tests',
    viewsTests: 'view_tests'
};

// Example React component using these route configurations
// If applicable, import React components and use the routes here
